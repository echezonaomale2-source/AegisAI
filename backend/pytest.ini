[pytest]
testpaths = tests
pythonpath = .
addopts = -q

[coverage:run]
source = brain,cognitive,knowledge,memory,research,evaluation,cv,dataset,core,storage,verification
omit =
    */.venv/*
    */tests/*

[coverage:report]
show_missing = true
skip_empty = true
fail_under = 0
